name: platform_channel_example
description: A new Flutter project using platform channels.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  encrypt: ^5.0.0
flutter:
  uses-material-design: true
